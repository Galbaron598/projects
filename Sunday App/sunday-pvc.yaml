apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sunday-data-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce  # Only one pod can mount this at a time
  resources:
    requests:
      storage: 1Gi  
  # storageClassName: standard 
