apiVersion: example.com/v1
kind: EtherealPod
metadata:
  name: second-ep
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: busybox
          image: busybox
          command: ["sh", "-c", "while true; do echo 'Second pod'; sleep 10; done"]
      restartPolicy: Always